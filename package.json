{
  "name": "event-scout",
  "private": true,
  "version": "0.3.0",
  "description": "Local events aggregator for photography scouting",
  "workspaces": [
    "server",
    "web",
    "crawler"
  ],
  "overrides": {
    "express": {
      "qs": "6.16.0"
    }
  },
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web && npm run build -w server && npm run build -w crawler",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
